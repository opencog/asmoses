#install header files
INSTALL(FILES
        ensemble.h
        ensemble_params.h
        metapopulation.h
        metapop_params.h

	DESTINATION

	"include/opencog/${PROJECT_NAME}/moses/metapopulation"
)
